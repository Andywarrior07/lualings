{
  "levels": [
    {
      "name": "00_fixture",
      "modules": [
        {
          "name": "basics",
          "exercises": [
            {
              "name": "passes",
              "path": "exercises/passes.lua",
              "mode": "compile",
              "hint": "Print something with print(\"passes\")."
            },
            {
              "name": "fails",
              "path": "exercises/fails.lua",
              "mode": "compile",
              "hint": "This one calls error(\"boom\") on propuse - fix it so the script runs cleanly."
            },
            {
              "name": "infinite_loop",
              "path": "exercises/infinite_loop.lua",
              "mode": "compile",
              "hint": "This one loops forever on purpose - replace it with something that terminates."
            }
          ]
        }
      ]
    }
  ]
}